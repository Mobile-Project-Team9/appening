import { View, Text } from 'react-native'
import React from 'react'

/* List of events instead of a map */
/* Less resource intensive, quick and clear listing */
/* Can sort by location, time etc. */

export default function EventList() {
  return (
    <View>
      <Text>EventList</Text>
    </View>
  )
}